# OpenTofu / Terraform runtime artifacts
# This repository holds reusable modules; execution state must never be tracked.
.terraform/
.terraform.lock.hcl
.tofu/
crash.log
*.tfstate
*.tfstate.*
*.backup
*.tfbackup
.terragrunt-cache/
